services:
  occ-dashboard-prod:
    build: .
    container_name: occ-dashboard-prod
    restart: unless-stopped
    env_file: .env.dashboard
    ports:
      - "127.0.0.1:5006:5000"
    networks:
      - occ-net

networks:
  occ-net:
    external: true
